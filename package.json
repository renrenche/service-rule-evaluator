{
  "name": "@rrc/service-rule-evaluator",
  "version": "3.0.0",
  "description": "service rule evaluator",
  "author": {
    "name": "wangshijun",
    "email": "wangshijun@renrenche.com"
  },
  "files": [
    "lib"
  ],
  "main": "lib/index.js",
  "keywords": [
    "rule",
    "evaluator",
    "engine"
  ],
  "devDependencies": {
    "chai": "^3.5.0",
    "eslint": "^3.17.1",
    "eslint-config-renrenche": "^1.3.0",
    "eslint-plugin-import": "^2.2.0",
    "gulp": "^3.9.0",
    "gulp-coveralls": "^0.1.0",
    "gulp-eslint": "^2.0.0",
    "gulp-exclude-gitignore": "^1.0.0",
    "gulp-istanbul": "^1.0.0",
    "gulp-line-ending-corrector": "^1.0.1",
    "gulp-mocha": "^2.0.0",
    "gulp-nsp": "^2.1.0",
    "gulp-plumber": "^1.0.0"
  },
  "eslintConfig": {
    "extends": "renrenche",
    "env": {
      "mocha": true
    }
  },
  "repository": "renrenche/service-rule-evaluator",
  "scripts": {
    "release:patch": "npm version patch && git push && git push --tags && npm publish",
    "release:minor": "npm version minor && git push && git push --tags && npm publish",
    "release:major": "npm version major && git push && git push --tags && npm publish",
    "prepublish": "gulp prepublish",
    "test": "gulp"
  },
  "license": "MIT",
  "publishConfig": {
    "access": "public"
  },
  "dependencies": {
    "debug": "^2.6.0",
    "lodash": "^4.17.4"
  }
}
